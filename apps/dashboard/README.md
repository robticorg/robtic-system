# @robtic/dashboard

Web dashboard for managing the Robtic Platform.

Status: **empty structure only** — no implementation yet.

Will consume:

- `libs/core` business services through `apps/api`
- `libs/types` shared DTOs
- `libs/events` websocket event definitions
