/** Embed text for the guild guard (unauthorized-guild protection). */
export const GUARD_MESSAGES = {
    embedTitle: "🛡️ Guild Guard",
    leftUnauthorizedGuild: "Left unauthorized guild on startup.",
    blockedUnauthorizedJoin: "Blocked join to unauthorized guild.",
    fieldGuild: "Guild",
    fieldGuildId: "Guild ID",
    fieldMembers: "Members",
} as const;
