/** Maximum characters accepted in the punishment proof note. */
export const PROOF_NOTE_MAX_LENGTH = 300;

/** Number of underscore-separated segments in a punish proof/shortcut custom id. */
export const PUNISH_CUSTOM_ID_SEGMENTS = 8;

/** Sentinel stored in a custom id when a punishment carries no duration/extra value. */
export const PUNISH_EXTRA_NONE = "none";

/** Maximum characters of a deleted message echoed into its audit embed. */
export const DELETED_MESSAGE_PREVIEW_LENGTH = 400;
